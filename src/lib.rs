// Lib root — exposes the crate's modules so integration tests under tests/
// (and any future tooling) can link against the same code the binary runs

pub mod app;
pub mod audio;
pub mod dsp;
pub mod ui;
pub mod annotation;
